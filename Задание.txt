<?php
$a = 5;
$b = '05';
var_dump($a == $b);  // Почему true? не строгая типизация, string к int 0 не учитывается
var_dump((int)'012345');  // Почему 12345? тоже самое что и выше
var_dump((float)123.0 === (int)123.0); // Почему false? float же в php не существует поэтому и false
var_dump((int)0 === (int)'hello, world'); // Почему true? если нет цифры то строка равна 0 при переведение типа
?>
